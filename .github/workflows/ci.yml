name: Apache helloworld webApp

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
    

jobs:
  
  Build:
    runs-on: ubuntu-latest

    steps:
      -  uses: actions/checkout@v2
      -  name: Build the Docker image
      -  run: docker build . --tag thegentl3m4n/test-webapp:v1.1       # define version here  


